 try:
        plt.scatter(X, Y)
        plt.xlabel("X-axis")
        plt.ylabel("Y-axis")
        plt.title("Scatter Plot")
        plt.show()
    except:
        print("There was an error, most likely have too many x variables.")
